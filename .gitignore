# Build artifacts
bin/
/documcp
/server
coverage.out

# Local settings with permissions (may contain sensitive patterns)
.claude/settings.local.json
.env
.devcontainer/.env

# Uploaded document storage (runtime data, not the Go source package)
/storage/

# Node modules (all locations)
node_modules/

# Grafana dashboard generator
grafana/dist/

# Frontend
frontend/src/api/generated/
# Don't ignore these - they should be committed:
# .claude/settings.json
# .claude/skills/
# .claude/agents/
# .claude/scripts/
# memory-bank/
# CLAUDE.md
